export const interfaceTypes = ["ethernet", "wireless", "unknown"] as const;
export const statusTypes = ["online", "working", "offline", "missing"] as const;
export const discoveryTypes = ["websocket", "manual", "other"] as const;
